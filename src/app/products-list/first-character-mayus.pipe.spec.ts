import { FirstCharacterMayusPipe } from './first-character-mayus.pipe';

describe('FirstCharacterMayusPipe', () => {
  it('create an instance', () => {
    const pipe = new FirstCharacterMayusPipe();
    expect(pipe).toBeTruthy();
  });
});
